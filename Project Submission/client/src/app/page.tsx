// The following code is used to setup a dashboard and uses @ to accomodate switching between folders in the package

import Dashboard from "@/app/dashboard/page";

export default function Home() {
  return <Dashboard />;
}